/** Must match config/app.php slug (used where boot is unavailable). */
export const PLUGIN_SLUG = "scheduled-page-reviews";

/** Must match config/app.php text_domain (for wp i18n string extraction). */
export const TEXT_DOMAIN = "scheduled-page-reviews";
